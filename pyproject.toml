[project]
name = "sheffield-hpc"
version = "1.0.1"
description = "Documentation for University of Sheffield HPC services"
readme = "README.md"
requires-python = ">=3.8"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = []
